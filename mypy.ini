[mypy]
# Exclude any directory that follows a pattern similar to the generated project slugs.
# Adjust the pattern according to your project's naming conventions.
exclude = {{cookiecutter.project_slug}}
